<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Listing details</title>
</head>

<body>
    <h2>Listing Details</h2>
    <h4>Title: <%= listing.title %>
    </h4>
    <h4>Description: <%= listing.description %>
    </h4>
    <h4>Price: ₹<%= listing.price.toLocaleString("en-IN") %>
    </h4>
    <h4>Country: <%= listing.location %>
    <h4>Country: <%= listing.country %>
    </h4>

    <img src="<%= listing.image.url %>" alt="<%= listing.title %>" width="300" />

</body>

</html>